## 0.1.0

- Initial release: Linear issue tracker backend for flutter_feedback_kit
- `LinearFeedbackBackend` implementing `FeedbackBackend`
- Creates Linear issues via GraphQL API with title, Markdown description, and metadata
- Optional project ID assignment
- `LinearBackendException` for structured error reporting
