name: flutter_feedback_kit_firebase
description: "Firebase Firestore + Storage backend for flutter_feedback_kit."
version: 0.1.0
homepage: https://github.com/SkyWalker2506/flutter_feedback_kit

environment:
  sdk: ^3.11.3
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_feedback_kit:
    path: ../..
  firebase_core: ">=3.0.0 <5.0.0"
  cloud_firestore: ">=5.0.0 <7.0.0"
  firebase_storage: ">=12.0.0 <14.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
