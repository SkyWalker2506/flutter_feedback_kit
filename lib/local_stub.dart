// Stub export for platforms that don't support dart:io (e.g. web).
// These classes are intentionally empty — LocalFeedbackBackend and
// FeedbackDevViewer rely on dart:io and cannot run on web.
//
// Consumers should only use `package:flutter_feedback_kit/local.dart`
// in code that is gated behind a non-web platform check.
